# Copy to .env.production locally if testing a production build, or
# (preferred) set VITE_API_URL as an Environment Variable in the Vercel
# project settings after the backend is deployed to Render, e.g.:
# https://finnet-assessment-api.onrender.com
VITE_API_URL=https://your-backend.onrender.com
